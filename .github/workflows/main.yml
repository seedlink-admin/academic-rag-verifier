name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit Health Endpoint
        run: curl -I https://academic-rag-verifier.streamlit.app/_stcore/health
